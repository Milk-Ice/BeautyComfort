import { createSlice } from "@reduxjs/toolkit";

const homeSilce = createSlice({
    name: "home",
    initialState: {
        bannerList: {}
    },
    reducers: {

    }
})
export default homeSilce.reducer