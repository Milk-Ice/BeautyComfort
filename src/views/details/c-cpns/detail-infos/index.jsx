import PropTypes from 'prop-types'
import React, { memo } from 'react'
import { InfoWrapper } from './style'

const DetaiInfos = memo((props) => {
  return (
    <InfoWrapper>
        
    </InfoWrapper>
  )
})

DetaiInfos.propTypes = {}

export default DetaiInfos