import React, { memo } from 'react'
import { HeaderCenterWrapper } from './style'

const HeaderCenter = memo(() => {
  return (
    <HeaderCenterWrapper>HeaderCente</HeaderCenterWrapper>
  )
})

export default HeaderCenter